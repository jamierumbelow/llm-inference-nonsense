[project]
name = "e01-kv-cache"
version = "0.0.0"
description = "the baseline Llama implementation with a KV cache"
requires-python = ">=3.13"
dependencies = [
    "harness",
    "torch>=2.14.0",
]

[tool.uv.sources]
harness = { workspace = true }

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
