[project]
name = "e00-baseline"
version = "0.0.0"
description = "the initial plain PyTorch Llama implementation"
requires-python = ">=3.13"
dependencies = [
    "harness",
    "torch>=2.14.0",
]

[tool.uv.sources]
harness = { workspace = true }

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
