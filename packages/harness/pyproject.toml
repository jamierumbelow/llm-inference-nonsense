[project]
name = "harness"
version = "0.0.0"
description = "shared runtime"
requires-python = ">=3.13"
dependencies = [
    "huggingface_hub",
    "safetensors>=0.8.0",
    "torch>=2.14.0",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
